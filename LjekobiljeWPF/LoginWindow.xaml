<Window x:Class="Ljekobilje.LoginWindow" x:Name="_LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ljekobilje"
        mc:Ignorable="d"
        Title="LoginWindow" Height="393" Width="663">
    <Grid>
        <Label Style="{DynamicResource LabelStyle}" Content="{DynamicResource Username}" HorizontalAlignment="Left" Margin="414,67,0,0" VerticalAlignment="Top"/>
        <TextBox Style="{DynamicResource TextBoxStyle}" Text="{Binding Username, Mode=TwoWay}" HorizontalAlignment="Left" Margin="386,93,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <Label Style="{DynamicResource LabelStyle}" Content="{DynamicResource Password}" HorizontalAlignment="Left" Margin="416,111,0,0" VerticalAlignment="Top"/>
        <TextBox Style="{DynamicResource TextBoxStyle}" Text="{Binding Password, Mode=TwoWay}" HorizontalAlignment="Left" Margin="386,137,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <Button Style="{DynamicResource AddButton}" Content="{DynamicResource Login}" Command="{Binding ElementName=_LoginWindow, Path=DataContext.LoginCommand}" Margin="386,179,158,159"/>

    </Grid>
</Window>
